/**
 * marathon-config.js
 * Constants only — change URLs and cache settings here, nowhere else.
 */

window.MARATHON_CONFIG = {
  ZOHO_API: '/server/nish_logic_function',
  CACHE_EXPIRY_MS: 4 * 24 * 60 * 60 * 1000  // 4 days
};
